<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_users_form" model="ir.ui.view">
        <field name="name">res.users.form.inherit.allowed.journals</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='account_security']" position="after">
                <page name="allowed_journals" string="Allowed Journals" groups="base.group_system">
                    <group>
                        <field name="allowed_journal_ids"
                               widget="many2many_tags"
                               options="{'no_create': True, 'no_create_edit': True}"/>
                    </group>
                    <div class="text-muted">
                        Adding journals here automatically limits the user to
                        the selected journals only.
                    </div>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
