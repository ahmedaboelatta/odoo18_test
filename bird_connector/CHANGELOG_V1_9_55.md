# v1.9.55

- Fix image downloads so files keep a real image extension instead of names like `bird-media-74`.
- Preserve Bird-provided filenames (including Arabic/Unicode) from Content-Disposition where available.
- Make document rows download directly from the inbox.
- Open selected conversations at the newest message even while media is still rendering.
