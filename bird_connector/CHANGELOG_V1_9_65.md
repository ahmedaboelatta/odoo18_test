# v1.9.65

- Close the full-screen media viewer when clicking the empty dark area around the image.
- Keep image clicks, zoom, wheel zoom, and drag-to-pan active without closing the viewer.
- Fix Odoo SCSS compilation fallback caused by CSS `min()` syntax in the attachment preview.
- Restore the intended styled image/document attachment preview cards.
