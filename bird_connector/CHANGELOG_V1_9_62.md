# v1.9.62

- Keep WhatsApp-style chat wallpaper visually fixed while the message list scrolls.
- Changed the message panel wallpaper attachment from `local` to `scroll`, so only messages move while the wallpaper remains stationary inside the chat pane.
