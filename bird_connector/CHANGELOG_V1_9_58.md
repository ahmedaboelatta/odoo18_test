# v1.9.58

- Capture image paste at document level so screenshots and clipboard images attach reliably.
- Added secure-context Clipboard API fallback and data-image HTML clipboard support.
- Message action menu now uses fixed viewport positioning so it always renders above the conversation/sidebar panes.
- Preserved the internal image clipboard fallback for HTTP deployments.
