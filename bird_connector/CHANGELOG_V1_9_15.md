# Bird Connector V1.9.15

- Drag & drop photos/documents directly into an open conversation.
- Contact list tag badges now use the configured tag colors.
- Auto-Routing Rules: channel/tag/keyword -> Team/Queue + optional assignee. Manual team assignment is preserved.
- Approved template Send Message always uses the template's own WhatsApp channel; channel is locked in the wizard.
- Bulk **Send WhatsApp Message** action on Bird Contacts with template preview and sequential one-by-one delivery.
- Template smart button shows all message logs sent from that template.
