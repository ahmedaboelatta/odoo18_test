# v1.9.51 - Media & Inbox UX

- Outbound signed media endpoint now accepts GET and HEAD so Bird can validate/fetch Odoo-hosted attachments.
- Added Quick Replies model, menu, team/channel scoping, + composer menu and `/` shortcut picker.
- Added per-message actions: Copy text, Copy Image (browser permitting), Download media/document.
- Fixed duplicate search clear icon by using a normal text input with the module clear button only.
- Fixed All conversations list action to reset Closed/other main filter as well as Team/Tag filters.
- Added selected conversation channel id for scoped Quick Replies.
